Review the August Pirraglia portfolio code quality.

Focus on:
- TypeScript correctness and strictness.
- CSS specificity, duplicated rules, hardcoded values that should be tokens, and layout fragility.
- GSAP best practices: transforms and opacity only, no layout-thrashing animation, ScrollTrigger creation order.
- Semantic HTML and accessible text-only interactions.

Run available lint/typecheck commands when possible and report exact output.
